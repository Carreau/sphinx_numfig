============
Installation
============

At the command line::

    $ easy_install sphinx_numfig

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sphinx_numfig
    $ pip install sphinx_numfig